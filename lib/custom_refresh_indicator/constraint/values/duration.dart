const Duration kIndicatorCancelDuration = Duration(milliseconds: 300);
const Duration kIndicatorSettlingDuration = Duration(milliseconds: 300);
const Duration kIndicatorRelizingDuration = Duration(milliseconds: 300);
